import { MovableDirective } from './movable.directive';

describe('MovableDirective', () => {
  it('should create an instance', () => {
    const directive = MovableDirective;
    expect(directive).toBeTruthy();
  });
});
