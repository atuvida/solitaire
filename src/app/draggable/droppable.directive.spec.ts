import { DroppableDirective } from './droppable.directive';

describe('DroppableDirective', () => {
  it('should create an instance', () => {
    const directive = DroppableDirective;
    expect(directive).toBeTruthy();
  });
});
