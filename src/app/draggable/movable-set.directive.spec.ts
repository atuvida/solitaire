import { MovableSetDirective } from './movable-set.directive';

describe('MovableSetDirective', () => {
  it('should create an instance', () => {
    const directive = new MovableSetDirective();
    expect(directive).toBeTruthy();
  });
});
